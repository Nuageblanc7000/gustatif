{% extends 'base.html.twig' %}

{% block title %}Log in!
{% endblock %}

{% block body %}
<div class="bg-common">
	<section class="container-form-common">
		<div class="commonForm">
        
            <img src="{{asset('/build/images/chefInscription.svg')}}" alt="chef">

			<form method="post" class="form">
				<div class="form-groups">
					<label for="inputEmail">Email</label>
					<input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
				</div>
				<div class="form-groups">
					<label for="inputPassword">{% trans %}Mot de passe{% endtrans %}</label>
					<input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>
				</div>

				<input
				type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

				{#
				        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
				        See https://symfony.com/doc/current/security/remember_me.html
				
				        <div class="checkbox mb-3">
				            <label>
				                <input type="checkbox" name="_remember_me"> Remember me
				            </label>
				        </div>
				    #}

				<button class="btn" type="submit">
					Connection
				</button>
			</form>
				{% if error %}
					<div class="form-message-errors">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
				{% endif %}
		</div>
	</section>
</div>
{% endblock %}
